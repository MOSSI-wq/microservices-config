server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false  # Un serveur Eureka ne s'enregistre généralement pas lui-même
    fetch-registry: false
