server:
  port: 8081

spring:
  application:
    name: produit-service
  datasource:
    url: jdbc:mysql://localhost:3306/produit?useSSL=false
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/

